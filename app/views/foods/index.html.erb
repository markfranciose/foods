All Foods

<%= form_tag("/foods", method: "GET") do %>
	<%= label_tag(:food, "Search for a food:") %>
	<%= text_field_tag(:food) %>
	<%= submit_tag %>
<% end %>
<br>
<br>
<% @foods.each do |food| %>
	<%= food.name %><br>
<% end %>
